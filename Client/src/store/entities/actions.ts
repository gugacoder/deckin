import { ActionTree } from 'vuex';
import { RootState } from '../types';
import { Entity } from './types';

export const actions: ActionTree<Entity, RootState> = {
  fetchData({ commit }): any {
    // TOOD: implementar...
  },
};
